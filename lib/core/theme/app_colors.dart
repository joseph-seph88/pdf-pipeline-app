import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Basic
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color transparent = Color(0x00000000);
  static const Color midnight = Color(0xff151A28);

  // Primary (Brown)
  static const Color primary = Color(0xFF2C1A0E);
  static const Color primaryLight = Color(0xFF8B5E3C);
  static const Color primaryDisabled = Color(0xFFAA9085);

  // Text (dark background)
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0x80FFFFFF);
  static const Color textTertiary = Color(0xFFBEC0C7);
  static const Color textMuted = Color(0xFF777B8A);
  static const Color textDisabled = Color(0xFF959DAE);
  static const Color textInverse = Color(0xFF000000);

  // Text (light background)
  static const Color textSub = Color(0xFF757575);
  static const Color textCaption = Color(0xFF9E9E9E);

  // Border
  static const Color borderDefault = Color(0xFFE0E0E0);

  // Status
  static const Color error = Color(0xFFEF5350);
}

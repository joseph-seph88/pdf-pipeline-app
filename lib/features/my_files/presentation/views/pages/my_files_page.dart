import 'package:flutter/material.dart';

class MyFilesPage extends StatelessWidget {
  const MyFilesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('내 파일')),
    );
  }
}
